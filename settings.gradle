rootProject.name = 'vertx-config-ext'
if (!JavaVersion.current().java8Compatible) {
  throw new IllegalStateException('''A vertx-config-ext:
                                    |  This needs Java 8,
                                    |  You are using something else,
                                    |  Refresh. Try again.'''.stripMargin())
}
